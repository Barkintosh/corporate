# Corporate

Static bilingual website for the Corporate World of Warcraft guild.

## Local preview

Run `node scripts/serve.mjs`, then open <http://localhost:8080>.

## GitHub Pages

The site has no build step. Publish the repository root from the default branch
in **Settings → Pages**, or serve the same files with any static host.
